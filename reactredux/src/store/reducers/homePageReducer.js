const INITIAL_STATE = {
    users: ['No User!']
};

export default function Reducer(state = INITIAL_STATE, action) {
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
};
